name: gigu_helper
description: Companion app para entregador - versão inicial
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0

flutter:
  uses-material-design: true